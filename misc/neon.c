#include<stdio.h>
#include<stdlib.h>
#include<string.h>

float sum_float(float a, float b)
{
	return (a+b);
}

int sum_int(int a, int b)
{
	return (a+b);
}

#if 0
int main(void)
{
	sum1(1.0, 2.0);
	return 0;
}
#endif
